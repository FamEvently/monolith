spring:
  application:
    name: monolith
    namespace: live

google:
  oauth2:
    client-id: 463363879926-8gdlvjqa7a9t13ct8olq646766l7959p.apps.googleusercontent.com
    secret: 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

  datasource:
    url: jdbc:mariadb://localhost:3306/famevently
    username: root
    password: root
    hikari:
      connection-test-query: "SELECT NOW()"
      maximum-pool-size: 20
      minimum-idle: 8
      connection-timeout: 10000

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      url:
      timeout: PT2S # 2 seconds
  threads:
    virtual:
      enabled: true
  lifecycle:
    timeout-per-shutdown-phase: 5s